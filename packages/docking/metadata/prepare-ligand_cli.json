{
  "name": "prepare-ligand",
  "type": "cli",
  "description": {
    "zh": "Prepare ligand for docking (SDF/SMILES -> PDBQT).\n\nArgs:\ninput_file (Path):\nPath to input ligand file (SDF, MOL2, PDB, SMI).\noutput_file (Path):\nPath to output PDBQT file.\nadd_hydrogens (bool, optional):\nWhether to automatically add hydrogens and generate 3D conformation. Defaults to True.",
    "en": "Prepare ligand for docking (SDF/SMILES -> PDBQT).\n\nArgs:\ninput_file (Path):\nPath to input ligand file (SDF, MOL2, PDB, SMI).\noutput_file (Path):\nPath to output PDBQT file.\nadd_hydrogens (bool, optional):\nWhether to automatically add hydrogens and generate 3D conformation. Defaults to True."
  },
  "params": [
    {
      "name": "--input-file",
      "type": "path",
      "required": true,
      "default": null,
      "description": {
        "zh": "Input ligand file (SDF, SMILES, PDB).",
        "en": "Input ligand file (SDF, SMILES, PDB)."
      }
    },
    {
      "name": "-o, --output",
      "type": "path",
      "required": true,
      "default": null,
      "description": {
        "zh": "Output PDBQT file.",
        "en": "Output PDBQT file."
      }
    },
    {
      "name": "--add-hydrogens",
      "type": "bool",
      "required": false,
      "default": "True",
      "description": {
        "zh": "Add hydrogens.",
        "en": "Add hydrogens."
      }
    }
  ]
}