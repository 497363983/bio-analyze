[project]
name = "{{name}}"
version = "0.1.0"
description = "Custom theme '{{name}}' for bio-analyze-plot"
dependencies = [
    "bio-analyze-plot",
]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
