# bio-analyze-plot

**bio-analyze-plot** ? `bio-analyze` ??????????????????????????????Publication-Ready???????????? `matplotlib` ? `seaborn` ?????? Nature ? Science ?????????????????????????????????

## ? ?? (Features)

- **?????**????? `nature`?`science` ? `default` ????????????????
- **???????**????? `.csv`?`.tsv`?`.txt`?`.xlsx` ? `.xls` ??????? `--sheet` ???????
- **?????**?????? `png`?`pdf`?`svg`?`jpg`?`tiff` ????????
- **????**????????????????????????
- **LaTeX ??**??????????????? LaTeX ?????? `$y = \sin(x)$`??
- **??? CLI**???????????????????????

## ?? ????? (Supported Plots)

### 1. ??? (Volcano Plot)

???????????DEG???????????????????

- **??**: `volcano`
- **????**:
  - `-x`: log2 Fold Change ?? (??: "log2FoldChange")
  - `-y`: P-value ?? (??: "pvalue")
  - `--fc-cutoff`: Fold Change ??
  - `--p-cutoff`: P-value ??

### 2. ??? (Bar Plot)

????????????????????????????????

- **??**: `bar`
- **????**:
  - `--error-bar-type`: ???????????? `SD` (???), `SE` (???), `CI` (?? 95% ????)?
  - `--error-bar-ci`: ???? `CI` ??????????? 95??
  - `--error-bar-max` / `--error-bar-min`: ???????????????
  - `--error-bar-capsize`: ??????? (??: 0.1)?
  - `--significance`: ??????????????? "Control,Treated"??
  - `--test`: ???????t-test_ind, t-test_welch, Mann-Whitney ???
  - `--text-format`: ??????? (star, full, simple, pvalue)?

### 3. ??? (Box Plot)

??????????????????????????????

- **??**: `box`
- **????**:
  - `-x`: ???? (Categorical)?
  - `-y`: ???? (Numerical)?
  - `--hue`: ??????
  - `--add-swarm`: ??????? (Swarmplot) ???????????
  - `--swarm-color`: ???? (?? ".25")?
  - `--swarm-size`: ???? (?? 3.0)?
  - `--significance`: ??????????????? "Control,Treated"??
  - `--test`: ???????t-test_ind, t-test_welch, Mann-Whitney ???
  - `--text-format`: ??????? (star, full, simple, pvalue)?

### 4. ?? (Heatmap)

???????????????????????????

- **??**: `heatmap`
- **????**:
  - `--cluster-rows` / `--cluster-cols`: ?????/????
  - `--z-score`: ? (0) ?? (1) ????

### 5. ?????? (PCA Plot)

????????????????????????????

- **??**: `pca`
- **????**:
  - `--transpose / --no-transpose`: ??????????????????? ?? x ????
  - `--hue`: ?????
  - `--cluster`: ??????/???????
  - `--n-clusters`: ?? KMeans ???????????? hue ?????

### 6. ??? (Line Plot)

????????????????

- **??**: `line`
- **????**:
  - `--hue`: ???????

### 7. ?? (Pie Chart)

????????????

- **??**: `pie`
- **????**:
  - `-x`: ???? (Categorical)?
  - `-y`: ???? (Numerical)?
  - `--explode`: ????????????????????
  - `--autopct`: ????? (?? "%1.1f%%")?
  - `--startangle`: ???? (?? 90)?
  - `--shadow`: ???????

### 8. ?????? (Chromosome Distribution Plot)

???? Reads ?????????????????????

- **??**: `chromosome` (?????? `rna_seq` ?????????????)
- **??**:
  - Track ?????????????
  - ???Strand +???????Strand -????
  - ?????????? (`color_pos`, `color_neg`)?
  - ????????? (`label_pos`, `label_neg`)?
  - ???????????

### 9. GSEA ??? (GSEA Enrichment Plot)

????????????GSEA?????????

- **??**: `gsea`
- **????**:
  - `--rank`: ????? (??: "rank")
  - `--score`: ???????? (??: "running_es")
  - `--hit`: ?????? (??: "hit", 0/1 ? True/False)
  - `--metric`: ?????? (????????????????)
  - `--color`: ???????? (??: Green)
  - `--hit-color`: ?????? (??: Black)
  - `--nes`: ??????? (??????)
  - `--pvalue`: P-value (??????)
  - `--fdr`: FDR/Adjusted P-value (??????)
  - `--show-border / --no-show-border`: ?????????????? (??: True)

## ?? ????? (Advanced Customization)

???????????????????????????

### ??????

- **??? (Volcano Plot)**:

  - `labels`: ??????? (??: `{"up": "Up", "down": "Down", "ns": "Not Sig"}`)?
  - `cutoff_line_kws`: ????? (??: `{"color": "grey", "linestyle": "--"}`)?
  - `palette`: ??????? (??: `{"Up": "red", ...}`)?
- **PCA ? (PCA Plot)**:

  - `ellipse_kws`: ?????? (??: `{"alpha": 0.2, "linestyle": "--"}`)?
  - `s`: ?????
- **??? (Bar Plot)**:

  - `err_color`: ??????
  - `cap_color`: ????????
  - `capsize`: ????????
- **???? (Chromosome Plot)**:

  - `zero_line_kws`: ?????

### ??????

?????? JSON/Python ????? `chart_specific_params` ?????????????

```json
{
  "name": "my_custom_theme",
  "chart_specific_params": {
    "volcano": {
      "alpha": 0.8,
      "s": 20,
      "labels": {"up": "Upregulated", "down": "Downregulated", "ns": "Insignificant"},
      "cutoff_line_kws": {"color": "black", "linewidth": 1}
    },
    "pca": {
      "s": 50,
      "ellipse_kws": {"alpha": 0.05}
    }
  }
}
```

## ?? ????? (Custom Themes)

????? `nature`?`science` ? `default` ??????????????????

### ?? JSON ??

???? JSON ????? `my_theme.json`???????????

```json
{
  "name": "dark_presentation",
  "style": "darkgrid",
  "context": "talk",
  "font": "Arial",
  "rc_params": {
    "lines.linewidth": 2.5,
    "axes.labelsize": 14
  }
}
```

?????

```bash
bioanalyze plot pca expression.csv --theme ./my_theme.json
```

### ?? Python ??

???? Python ????? `my_theme.py`?????? `PlotTheme` ???

```python
from bio_analyze_plot.theme import PlotTheme

THEME = PlotTheme(
    name="custom_py",
    style="whitegrid",
    context="poster",
    rc_params={
        "font.size": 16,
        "axes.titlesize": 20
    }
)
```

?????

```bash
bioanalyze plot pca expression.csv --theme ./my_theme.py
```

### ?? Python ? (??)

????????? Python ????

??????

```python
# my_company_theme/__init__.py
from bio_analyze_plot.theme import PlotTheme

THEME = PlotTheme(
    name="mycompany",
    style="white",
    palette="pastel"
)
```

????????????????

```bash
pip install my-company-theme
bioanalyze plot pca expression.csv --theme my_company_theme
```

`bio-plot` ??????? `my_company_theme` ?????? `THEME` ???

?????????????? `bio_analyze_plot_theme_` ????? `bio_analyze_plot_theme_mytheme`????????????????

```bash
bioanalyze plot pca expression.csv --theme mytheme
# ??? --theme bio_analyze_plot_theme_mytheme
```

## ?? ???? (Usage)

### ?????

```bash
# ?? CSV ?????? PNG
bioanalyze plot volcano result.csv -o volcano.png --fc-cutoff 1.5 --p-cutoff 0.01 --title "Volcano Plot ($p < 0.01$)"
```

### ?? PCA ????????

```bash
# ???????? Group ???????
bioanalyze plot pca expression.csv --hue Group --cluster -o pca.png
```

### ?????????? (?? SE)

```bash
# ?? Excel ???? Group ??
bioanalyze plot bar data.xlsx -x Treatment -y Value --hue Group --error-bar-type SE -o bar_plot.pdf
```

### ?????????? (?? 99% CI)

```bash
# ?? Excel ???? Group ??
bioanalyze plot bar data.xlsx -x Treatment -y Value --hue Group --error-bar-type CI --error-bar-ci 99 -o bar_plot_ci.pdf
```

### ????????????

```bash
# ?? Control ? Treated ?
bioanalyze plot bar data.csv -x Group -y Value --significance "Control,Treated" --test t-test_ind -o bar_sig.png
```

### ??????????????

```bash
# ?????????????????
bioanalyze plot box data.csv -x Group -y Value --add-swarm --significance "A,B" -o box_plot.png
```

### ??????

```bash
# ???? Z-score ??????
bioanalyze plot heatmap expression.tsv --index-col GeneID --z-score 0 -o heatmap.png
```

## ??? ?? (Development)

```bash
# ??????
pytest packages/plot/tests
```
